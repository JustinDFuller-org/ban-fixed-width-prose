{
  "name": "@justindfuller/ban-fixed-width-prose",
  "version": "1.2.1",
  "description": "Detect and ban hard-wrapped prose in Markdown and text files",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/JustinDFuller-org/ban-fixed-width-prose.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "ban-fixed-width-prose": "./bin/ban-fixed-width-prose.js"
  },
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "coverage": "c8 --reporter=text --reporter=cobertura --check-coverage --lines=90 --branches=90 node --test",
    "build": "ncc build src/action.js --minify -o dist",
    "check-action": "node scripts/validate-action.mjs",
    "check-release": "node scripts/validate-release.mjs",
    "check-package": "node scripts/validate-package.mjs",
    "check-bundle": "npm run build && git diff --exit-code -- dist/index.js",
    "build-plugins": "node scripts/build-plugins.mjs",
    "check-plugins": "node scripts/validate-plugins.mjs",
    "smoke-codex": "node scripts/smoke-codex.mjs",
    "smoke-claude": "node scripts/smoke-claude.mjs",
    "check": "npm test && npm run coverage && npm run check-action && npm run check-release && npm run check-package && npm run check-bundle && npm run build-plugins && npm run check-plugins"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@vercel/ncc": "^0.38.3",
    "c8": "^12.0.0"
  }
}
